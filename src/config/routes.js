const routes = {
    home: '/',
    login: '/login',
    register: '/register',
    profile: '/profile/:userId',
    createPost: '/create-post',
    savedPosts: '/saved-posts',
    setting: '/setting',
    notFound: '*',
};

export default routes;
